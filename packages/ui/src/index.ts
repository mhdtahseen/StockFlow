/**
 * @finventree/ui
 *
 * Shared UI component library for all Finventree apps.
 * Populated incrementally as shadcn components are extracted from apps/app in Phase 2.
 */

export const UI_VERSION = '0.0.1';

// Phase 2: export Button, Card, Input, Badge, Dialog, etc.
// These will be the shadcn/ui components shared across app, admin, and web.
