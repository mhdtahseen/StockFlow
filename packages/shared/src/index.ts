/**
 * @finventree/shared
 *
 * Shared utilities, types, and Supabase client used across all Finventree apps.
 * Populated incrementally as code is extracted from apps/app in Phase 2.
 */

export const FINVENTREE_VERSION = '0.0.1';

// Phase 2: export supabase client, auth helpers, shared types, utility functions
