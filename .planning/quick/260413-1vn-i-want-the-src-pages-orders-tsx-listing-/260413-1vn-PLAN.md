---
must_haves:
  - id-prominence: Order ID is visually dominant over customer name
  - card-fidelity: Card layout matches PurchaseOrders.tsx exactly (rounded-24px, glass headers)
  - logic-separation: Sales Order logic (statuses, filters) remains unchanged
---

# Quick Plan 260413-1vn: Refine Orders UI Parity

This plan outlines the surgical UI updates to `Orders.tsx` to achieve visual parity with the modern card design found in `PurchaseOrders.tsx`.

## Tasks

### 1. Typography & Hierarchy Meta
- **Action**: Increase Order ID font size to `text-base` and weight to `font-black`.
- **Action**: Adjust Customer Name to `text-[13px]`/`text-xs` with lighter prominence (`text-slate-500`).
- **Files**: [Orders.tsx](file:///Users/taaha/Desktop/Projects/Products/StockFlow/src/pages/Orders.tsx)
- **Verify**: Inspect card text sizes match the "ID Priority" requirement.

### 2. Card Layout & Structure
- **Action**: Ensure the card container padding and inner spacing (e.g., gap between status row and ID row) matches `PurchaseOrders.tsx`.
- **Action**: Verify `shadow-[0_4px_12px_rgba(0,0,0,0.02)]` and hover states are identical.
- **Files**: [Orders.tsx](file:///Users/taaha/Desktop/Projects/Products/StockFlow/src/pages/Orders.tsx)
- **Verify**: Hover one card to confirm smooth scaling and shadow elevation.

### 3. Glassmorphism Header Audit
- **Action**: Verify the z-index and backdrop blur of the sticky header matches the PO implementation.
- **Files**: [Orders.tsx](file:///Users/taaha/Desktop/Projects/Products/StockFlow/src/pages/Orders.tsx)
- **Verify**: Scroll through the list to ensure transparency/blur effects are consistent.
